Library of Drm features, including cta plugin.

This library is pre-built by MediaTek internal.

WHAT IT DOES?
=============
libdrmctaplugin will be used by drm, it contains cta interface called by drm manager.
Without them, drm cta feature will not work.

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:
1. libicui18n.so
2. libicuuc.so
3. libutils.so
4. libcutils.so
5. libdl.so
6. libandroid_runtime.so
7. libdrmframework.so
8. libstlport.so
9. libbinder.so

and the following libs from MediaTek:
1. libdrmmtkutil.so
2. libmtk_drvb.so

All source/dependency modules of this module are already put in
'system/lib' folder.

HOW TO USE IT?
==============
libdrmctaplugin provides interface of cta plugin, it is built library.
