library of Drm features, including control which process can access drm.

This library is pre-built by MediaTek internal.

WHAT IT DOES?
=============
libdrmmtkwhitelist constrols which process can access drm, if process is not in the table,
it will not have right to access drm. Without this, any process can be access drm.

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:
1. libutils.so
2. libcutils.so
3. libstlport.so

and the following libs from MediaTek:


All source/dependency modules of this module are already put in
'system/lib' folder.

HOW TO USE IT?
==============
libdrmmtkwhitelist controls process which is allowed to access drm, if need more process access drm,
only add process name in this table.
